fastapi==0.115.12
uvicorn==0.34.0
python-multipart==0.0.20
# Lightweight neural-network OMR engine (replaces Audiveris / JVM dependency).
# Runs on CPU; installs via pip.  Requires the poppler system library for PDF
# rasterisation (apt install poppler-utils / brew install poppler).
oemer>=0.1.6
pdf2image>=1.17.0
pytest==8.3.5
httpx==0.28.1
