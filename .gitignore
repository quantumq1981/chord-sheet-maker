# Dependencies
node_modules/

# Build output
dist/

# AlphaTab static assets are tracked in public/ (served directly; no Vite plugin)
# dist/ (build output) is excluded below

# TypeScript incremental build info
*.tsbuildinfo

# Vite-compiled config artefacts (generated when tsc processes vite.config.ts)
vite.config.js
vite.config.d.ts

# Local environment files
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
