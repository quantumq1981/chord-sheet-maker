// Global test setup — imported once before every test file via vite.config.ts
// `test.setupFiles`.  Extends Vitest's `expect` with DOM-specific matchers
// from @testing-library/jest-dom (e.g. toBeInTheDocument, toHaveTextContent).
import '@testing-library/jest-dom';
